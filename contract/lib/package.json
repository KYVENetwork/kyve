{
  "name": "@kyve/contract-lib",
  "version": "0.1.0-alpha.6",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "dependencies": {
    "@kyve/contract": "0.1.0-alpha.3",
    "arweave": "^1.10.13",
    "smartweave": "^0.4.29"
  }
}
